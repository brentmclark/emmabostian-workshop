import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { SignUpModal } from "./Modal";

<Meta title="Design System|Modals" component={SignUpModal} />

# Modals

Modals are used to overlay content on top of the current page when a contextual shift has occurred.

## Usage

Tell you later

## Sign Up Modal

Primary buttons are used as a call to action and indicate the most important action on a page.

<Preview withToolbar>
  <Story name="primary">
    <SignUpModal show={true} />
  </Story>
</Preview>
